# ==============================================================================
# C4lendar Bot - Environment Configuration Template
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real values to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# Telegram Bot Configuration
# ------------------------------------------------------------------------------
# Get this from @BotFather on Telegram after creating your bot
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890

# Your numeric Telegram user ID (get from @userinfobot)
# This user will have admin access to the bot
ADMIN_USER_ID=123456789

# ------------------------------------------------------------------------------
# Google OAuth 2.0 Configuration
# ------------------------------------------------------------------------------
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Application type: Web application
# 3. Add authorized redirect URI: https://your-worker.workers.dev/oauth/callback
# 4. Enable Google Calendar API in your project

GOOGLE_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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45

# ------------------------------------------------------------------------------
# Groq API Configuration
# ------------------------------------------------------------------------------
# Get your API key from: https://console.groq.com/keys
# Used for natural language parsing of calendar event text
GROQ_API_KEY=gsk_abc123def456ghi789jkl012mno345pqr678stu901vwx234yz

# ------------------------------------------------------------------------------
# Cloudflare Workers Configuration
# ------------------------------------------------------------------------------
# Your deployed Cloudflare Worker URL (used for OAuth redirect)
# Format: https://worker-name.subdomain.workers.dev
WORKER_URL=https://your-worker-name.your-subdomain.workers.dev

# ------------------------------------------------------------------------------
# Security Configuration
# ------------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
# Used to encrypt Google OAuth refresh tokens in storage
ENCRYPTION_KEY=abcdefghijklmnopqrstuvwxyz123456

# Secret for HMAC signing OAuth state parameters (prevents CSRF)
# Can be the same as ENCRYPTION_KEY or generate separately
OAUTH_STATE_SECRET=abcdefghijklmnopqrstuvwxyz123456

# ------------------------------------------------------------------------------
# Optional: Legacy Google Apps Script Fallback
# ------------------------------------------------------------------------------
# If migrating from a previous GAS implementation, you can keep it as fallback
# Leave blank if not using GAS
GAS_WEBAPP_URL=

# ------------------------------------------------------------------------------
# Cloudflare KV Namespace
# ------------------------------------------------------------------------------
# Create with: wrangler kv:namespace create "EVENTS"
# Then update the ID in wrangler.toml
# This is the KV namespace ID for storing calendar event mappings
KV_NAMESPACE_ID=your_kv_namespace_id_here

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
# Default timezone for calendar events (IANA timezone format)
TIMEZONE=Asia/Singapore

# Default event duration in minutes when not specified
DEFAULT_DURATION_MINUTES=60

# OAuth feature flag (true/false)
USE_OAUTH=true

# Enable Google Apps Script fallback (true/false)
ENABLE_GAS_FALLBACK=false
